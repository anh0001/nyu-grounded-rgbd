model = "gpt-5.4"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"

developer_instructions = """
Stay in exploration mode.
Trace the real execution path, cite files and symbols, and prefer targeted reads over broad scans.
Prioritize evidence around experiment config composition, dataset metadata, prompt chunking, model wrappers, and the proposals -> dedup -> rasterize pipeline.
Avoid proposing fixes unless the parent agent asks for them.
"""
